import { PageViewDirective } from './page-view.directive';

describe('PageViewDirective', () => {
  it('should create an instance', () => {
    const directive = new PageViewDirective();
    expect(directive).toBeTruthy();
  });
});
